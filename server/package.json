{
  "name": "qivalabs-api-server",
  "version": "1.0.0",
  "description": "QivaLabs lead capture API — deployed to Render, called from the Next.js frontend on Vercel.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
